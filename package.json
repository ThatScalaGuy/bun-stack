{
    "name": "bun-stack",
    "private": true,
    "workspaces": [
      "backend", "frontend"
    ],
    "scripts": {
      "dev": "concurrently \" cd backend && bun run dev\" \"cd frontend && bun run dev\""
    },
    "devDependencies": {
      "@types/bun": "latest",
      "concurrently": "latest"
    },
    "peerDependencies": {
      "typescript": "^5.8.2"
    }
  }